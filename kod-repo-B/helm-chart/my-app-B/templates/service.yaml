apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-app-B.fullname" . }}
  labels:
    {{- include "my-app-B.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-app-B.selectorLabels" . | nindent 4 }}
