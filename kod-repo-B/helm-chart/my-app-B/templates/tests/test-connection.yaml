apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-app-B.fullname" . }}-test-connection"
  labels:
    {{- include "my-app-B.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-app-B.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
