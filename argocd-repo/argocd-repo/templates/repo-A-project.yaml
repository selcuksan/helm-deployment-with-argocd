apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: repo-A
  namespace: {{ .Values.argocd.namespace }}
spec:
  destinations:
    - name: '*'
      namespace: "repo-A"
      server: {{ .Values.env.local.url }}