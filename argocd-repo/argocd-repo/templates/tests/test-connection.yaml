apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argocd-repo.fullname" . }}-test-connection"
  labels:
    {{- include "argocd-repo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argocd-repo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
